import type { Plugin } from "vue";
import { PApplication, PChart, PGrid, PInfoList, PKanban, PSwipe, PTitle } from "@lead-portal/components";

export default [PApplication, PChart, PGrid, PInfoList, PKanban, PSwipe, PTitle] as Plugin[];
